<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.filafixml.fragment.HomeFragment"
        android:label="home"
        tools:layout="@layout/fragmenthome">

        <action
            android:id="@+id/action_homeFragment_to_detailFragment"
            app:destination="@id/detailFragment"/>
        <action
            android:id="@+id/action_homeFragment_to_languageFragment"
            app:destination="@id/languageFragment"/>
    </fragment>

    <fragment
        android:id="@+id/detailFragment"
        android:name="com.example.filafixml.fragment.DetailFragment"
        android:label="detail"
        tools:layout="@layout/fragmentdetail">

        <argument
            android:name="filmId"
            app:argType="integer"/>
    </fragment>

    <fragment
        android:id="@+id/languageFragment"
        android:name="com.example.filafixml.fragment.LanguageFragment"
        android:label="language"
        tools:layout="@layout/fragmentlanguage"/>

</navigation>